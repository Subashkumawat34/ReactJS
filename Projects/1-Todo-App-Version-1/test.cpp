#include <iostream>
using namespace std;

int bsearch(int target, int low, int high, int arr)
{
}

int main()
{
  int n;
  int arr[n];
  cout << "Enter the sorted array";
  for (int i = 0; i < n; i++)
  {
    cout << "Enter the " << i + 1 << "number";
    cin >> arr[i];
  }
  for (int i = 0; i < n; i++)
  {
    cout << arr[i];
    cout << endl;
  }
}